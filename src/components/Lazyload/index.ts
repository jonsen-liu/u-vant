// import { createApp } from 'vue'

// const app = createApp({})

// app.directive('Lazyload', {
// 	// 当被绑定的元素挂载到 DOM 中时……
// 	mounted () {
// 		// 聚焦元素
// 		// el.focus()
// 		console.log(123)
// 	}
// })

export const Lazyload = {
	mounted () {
		console.log(123)
	}
}
